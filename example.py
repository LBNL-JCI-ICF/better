import better

# Execute the line below to demonstrate the analysis for a single building
better.demo.run_single(bldg_id = 1, saving_target=2, cached_weather=False)

# Execute the line below to demonstrate the analysis for a single building
# better.demo.run_batch(start_id = 1, end_id = 10, saving_target=2, cached_weather=False, batch_report=False)